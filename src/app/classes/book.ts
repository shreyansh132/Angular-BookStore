import { NgIf } from '@angular/common';

export class Book {
    id: number;
    name: string;
    image_url: string;
    price: number;
    author: string;
    description: string;
    rating: number;
    category_id: number;
    category: string;
    publicationDate: string;
    publisher: string;
    active: number;
}
